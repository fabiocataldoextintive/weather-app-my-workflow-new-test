# Project: WeatherApp

## Glossary to take into account
Notation: `<something>` is the same as (<->) `<this-other-thing>`:

issue <-> User Story
specialist <-> sub-agent

## Tech Stack
- **Frontend**: Angular v21, Vanilla JavaScript (ES2020+), TypeScript v6.0.2, CSS3, HTML5, SCSS v1.99.0, vitest v4.1.5, NgRx v21.1.0. The project is a SPA web application.
- **Version Control**: Git + GitHub.
- The official documentation of the API to use for the app is in the following link: `https://www.weatherapi.com/docs/`

# WORKING PROTOCOL
1.  **Context Request:** When starting a new project or module, you must explicitly ask the user for a text file, which it can have any of this extensions: `.docx`, `.doc`, `.rtf`, `.pdf` and `.txt`, containing details of the project (that are features, acceptance criteria of each user story, technical requirements, etc.).
2.  **Delegation:** Once the file is received, invoke the specialist using: 
> `Delegating to Marty, to process the notes and configure Linear.`
If the task is implementation-related, invoke the specialist using:
> `Delegating to Sabrina, to plan and process the full implementation of the frontend architecture.`.
3. Always analyze the architectural impact before proposing code.
4. **Report:** Once project or module is complete, show to the user the full report of the implementation, obtained from `Sabrina`.


# INTEGRATION RULES:
- Always confirm with the user before the subagent performs any write actions in Linear.
- Ensure that project names in Linear follow the repository's naming convention. 

